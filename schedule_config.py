import time
import subprocess

while True:
    # Substitua a linha abaixo pelo comando ou script que você deseja executar
    subprocess.run(["L:\\GitHub\\LineBy_Python\\init.cmd"])

    # Dorme por 1 segundo antes de repetir
    time.sleep(1)
